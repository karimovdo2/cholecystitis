# ────────── UI ──────────
streamlit==1.46.0          # проверено: есть готовые wheels для cp312/cp313

# ────────── ML / DS ─────
pandas>=2.1                # 2.3.0-2.3.1 имеют wheels под 3.12/3.13
numpy>=1.26                # catboost ≥ 1.2.8 требует ≥1.26
scikit-learn>=1.4.2        # wheels вплоть до Python 3.13
catboost>=1.2.8            # начиная с 1.2.8 на PyPI есть wheels cp312/cp313
shap==0.46.0               # последняя версия с бинарными колёсами под 3.12+

# ────────── визуализация/отчёты ──────────
matplotlib>=3.9
seaborn>=0.13
openpyxl>=3.1.2            # чтение/запись Excel при необходимости

# ────────── (опционально, но полезно) ──────────
# rich улучшает форматирование трейсбеков, уже подтягивается самим Streamlit,
# но явно фиксируем версию, чтобы избежать неожиданных апдейтов.
rich>=14.0.0
